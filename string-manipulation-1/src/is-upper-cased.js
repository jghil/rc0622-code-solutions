/* exported isUpperCased */

// identify each index
// check if the characters in word are all uppercased
// if so, return true
// if not, return false

function isUpperCased(word) {
  return word === word.toUpperCase();
}
// check to see if origianl string is strict equal to uppercase string and return it
