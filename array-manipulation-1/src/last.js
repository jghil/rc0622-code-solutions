/* exported last */

// check the indexes of the array
// save the value of the last index of the array into a variable
// return the variable

function last(array) {
  return array[array.length - 1];
}

// check the indexes of the array
// return the value of the last index of the array
